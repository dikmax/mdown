library mdown.generators.embed_blns_tests;

/// Embedded test annotation
class EmbedBlnsTests {
  /// File path
  final String path;

  /// Url to repo
  final String url;

  /// Constructor
  const EmbedBlnsTests(this.path, this.url);
}
